services:
  mongodb:
    image: mongo:7
    container_name: ravenfold-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
